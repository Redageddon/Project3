@using API.DataModels.Food
@model MealsDataModel
@* @model List<MealsModel> *@

@{
    ViewData["Title"] = "Meals";
}

@section Styles
{
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="stylesheet" href="~/css/Meals.css"/>
    <link rel="stylesheet" href="/css/Cards.css" />
}

<div class="recipes-header py-3">
    <div class="container">
        <h1 class="title h3 mb-1">Meals</h1>
        <p class="subtitle mb-0">Browse the Meals</p>
    </div>
</div>

<div class="card-container">
    <div class="row g-3">
        @* @foreach (MealsModel meal in Model.Meals) *@
        @* { *@
        @*     <div class="col-12 col-sm-6 col-lg-4"> *@
        @*         @await Html.PartialAsync("~/Views/Shared/_MealsCard.cshtml", meal) *@
        @*     </div> *@
        @* } *@
    </div>
</div>
